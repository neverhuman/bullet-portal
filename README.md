# bullet-portal

Operations portal for Bullet Farm. Projection only.

```bash
npm install
just fast
npm run dev
```

Point `VITE_BULLET_API` at `bullet-farmd` (default proxy: `http://127.0.0.1:7420`).
